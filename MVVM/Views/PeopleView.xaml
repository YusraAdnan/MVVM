<UserControl x:Class="MVVM.Views.PeopleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:PeopleViewModel/>
    </UserControl.DataContext>
    <StackPanel Background="White">
        <Label Content="{Binding Name}" FontSize="18" Width="779" />
        <Label Content="{Binding Age}" FontSize="18" Height="44" Width="779" />
        <Label Content="{Binding Address}" FontSize="18" Height="44" Width="775" />

        <StackPanel Orientation="Horizontal" Width="778">
            <Button Content="Previous" Command="{Binding Previous}" Height="41" Width="96" FontSize="18" FontWeight="Bold" />
            <Button Content="Next" Command="{Binding Next}" Margin="10" Height="41" Width="110" FontSize="18" FontWeight="Bold" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0" Width="784">
            <TextBox Width="222" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Height="42" />
            <Button Content="Search" Command="{Binding Search}" Height="46" Width="144" FontSize="20" FontWeight="Bold" />
        </StackPanel>
    </StackPanel>
</UserControl>